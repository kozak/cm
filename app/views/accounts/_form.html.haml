= simple_form_for [@bank, @account], :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :currency, :collection => Currency.all, :include_blank => false
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to_cancel :banks
