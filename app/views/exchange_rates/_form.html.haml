= simple_form_for @exchange_rate, :html => { :class => 'form-horizontal' } do |f|
  = f.input :rate
  = f.input :currency
  = f.input :date
  = f.input :table
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to 'Cancel', exchange_rates_path, :class => 'btn'
